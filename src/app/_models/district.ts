import { Authority } from "./authority";

export class District {
    id: number;
    code: string;
    name: string;
    description: string;
    state: string;
    active: boolean;
    createdBy: string;
    createdDate: string;
    lastModifiedBy: string;
    lastModifiedDate: string;
}
